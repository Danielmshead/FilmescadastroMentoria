@model IEnumerable<FILMECRUD.Models.FilmeDetalhe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FilmeDescricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FilmeNome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FilmeRefGenero.FilmeRefGeneroName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FilmeDescricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FilmeNome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FilmeRefGenero.FilmeRefGeneroName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FilmeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FilmeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FilmeID })
        </td>
    </tr>
}

</table>
